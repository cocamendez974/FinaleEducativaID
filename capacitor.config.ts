import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farasi.finale',
  appName: 'Finale Educativa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
